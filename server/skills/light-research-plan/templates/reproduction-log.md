<!--
复现日志模板（templates/reproduction-log.md）。
配套 SKILL「复现已有论文」五步协议的第 4 步——逐次记录"改了什么/得到的数/与目标差/下一步假设"。
一次复现尝试一行，按时间累加；与 reproducibility-checklist.md（环境/版本勾选）互补：那张管"配置可复现"，本表管"复现过程留痕"。
-->

# 复现日志 · {{论文标题 / arXiv id}}

## 复现目标（可验收，禁"复现这篇论文"这种无法验收的目标）
- **目标指标 + 数值**：{{如 Table 2 的 top-1 = 76.3%，±多少算复现成功}}
- **偏差预算**：{{允许的差距，如 ±0.5% 内算成功；超出走失败三分归因}}
- **资产盘点**：{{代码开源否 / 权重 / 数据 / 配置 / 缺哪些（缺的列为风险）}}

## 复现尝试日志（一次尝试一行，按时间累加）

| # | 日期 | 改了什么（相对上次/原文的差异） | 得到的数 | 与目标差 | 下一步假设（下次改什么、为什么） |
|---|------|------------------------------|---------|---------|--------------------------------|
| 1 | {{YYYY-MM-DD}} | {{按原文默认配置首跑}} | {{top-1=73.1%}} | {{-3.2%}} | {{怀疑 LR warmup 没对齐，下次按原文 5 epoch warmup}} |
| 2 | | | | | |
| 3 | | | | | |

> 每行的"下一步假设"必须可执行、可验证（改一个变量看一个结果），别写"再调调"。
> 改动尽量单变量（一次只改一处），否则差距归因被污染（同 m02 防泄漏/safe_split 单变量精神）。

## 失败归因（达不到偏差预算时，三分归因，禁笼统"没复现出来"）
- **实现差异**：{{我的实现与原文不一致处（如优化器/初始化/数据增强），可定位的}}
- **数据差异**：{{数据版本/预处理/划分与原文不同}}
- **原文问题**：{{怀疑原文本身不可复现/描述不全——须多方交叉验证，勿轻率公开指控，走 a10 research-ethics}}

## 结论
- [ ] 复现成功（在偏差预算内）　- [ ] 部分复现（说明哪些项达标）　- [ ] 未复现（归因 + 证据）
- 复现结果作 baseline 时，数值与口径交 m06（light-result-analysis）；代码/调试走 a03（light-backend-coding）。
